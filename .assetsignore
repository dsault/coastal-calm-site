# Cloudflare Workers assets. Git pack files are > 25 MiB and fail deploy.
.git
.git/**
**/.git/**

node_modules
.wrangler
.github
*.log

# Menu and other PDFs must not be published as downloadable files.
*.pdf
**/*.pdf
